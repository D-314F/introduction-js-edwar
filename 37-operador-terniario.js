/* El operador terniario es una forma corta y elegante de una estructura if ...else*/

// se llama terniario por que tienen tres operadores, observemos la sintaxis 
// condicion ? valorSiVerdadero : valorSiFalso 


// Ejemplo 1

let age = 15; 
let messages = age  >= 18 ? "Eres mayor de edad" : "Eres menor de edad"
console.log(messages)