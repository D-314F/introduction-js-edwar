// Fabian Guzman villegas
//
const numbers = [0, 12, 8, 9, 5, 8, 70, 120];

//  Agregar elementos a mi arreglo, pero con este metodo
//  se requiere conocer la longitud del arreglo
// const numbersLenght = numbers.length
// console.log(numbersLenght);

// numbers [numbersLenght] = 100;

// console.log(numbers);

// Con el método push se agregan elementos al final del arreglo
// numbers.push(700, 800);
// console.log(numbers);

// console.table(numbers);

// Método unshift para agregar elementos a un arreglo 
numbers.unshift(-3, -2, -1, );

//  Método pop para eliminar el utimo elemento de mi arreglo 
// numbers.pop();

//  Método shift para eliminar el primer elemento de mi arreglo 
// numbers.unshift();

// Método splice elimina todos los elementos a partir del indice asignado 
// numbers.splice(5);


index = numbers.indexOf(0);
console.table(numbers)